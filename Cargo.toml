cargo-features = ["profile-rustflags"]

[workspace]
members = ["core", "frontend", "cli", "renderer", "db", "macros"]
default-members = ["frontend"]
resolver = "3"

[workspace.package]
version = "0.2.6"
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/EmIsGreat/Monsoon-Emulator"
keywords = ["nes", "emulator", "nintendo"]
categories = ["emulators", "game-development"]

[workspace.dependencies]
monsoon-core = { version = "0.2.6", path = "core" }
monsoon-default-renderers = { version = "0.2.6", path = "renderer" }
monsoon-db = { version = "0.2.6", path = "db" }
monsoon-macro = { version = "0.2.6", path = "macros" }

[profile.full_release]
inherits = "release"
codegen-units = 1
lto = "fat"

[profile.max_performance]
inherits = "full_release"
rustflags = ["-C", "target-cpu=native"]

[profile.release]
strip = true
panic = "abort"
incremental = true

[profile.profiling]
panic = "unwind"
inherits = "release"
strip = "none"
debug = "full"
split-debuginfo = "unpacked"  # Ensures symbols are separate
